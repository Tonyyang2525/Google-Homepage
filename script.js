function searchF() {

    let searchInput = document.getElementById('search').value;
    //document.getElementById('') retrieves the input recieved by user.
    //.value returns the user input 
    //getting the element based on the id input which search. 
    //once element is obtained, you want to get the user input, which is the .value
    alert(searchInput);

}

